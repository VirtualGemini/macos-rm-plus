# Repository toolchain compatibility

Repository builds and tests must fail before compiling project targets when the active Swift
compiler, macOS SDK, and Apple `Testing.framework` cannot be used together.
