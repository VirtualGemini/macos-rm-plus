# rmp Help Contract

The command-line help interface has not been implemented. Until the CLI implementation ticket is
complete, `rmp` exits with a scaffold error and does not claim support for any operational option.

When CLI arguments, output, or exit codes are implemented or changed, this document must be updated
in the same pull request alongside the README, product specification, domain language, and changelog.
